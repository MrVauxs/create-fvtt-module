# create-fvtt-module

## Installation

To install this package, run `bun add https://github.com/MrVauxs/create-fvtt-module --global` and then run `create-fvtt-module` in your terminal.

Once this package is published, you can run it via `bunx create-fvtt-module`.
